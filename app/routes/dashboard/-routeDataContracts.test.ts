// These tests validated Convex route data contracts (convex/react prewarmQuery).
// They are no longer applicable after the migration to a self-hosted Fastify API
// with TanStack Query. New route data tests should validate TanStack Query key
// contracts against the REST API endpoints.
//
// TODO: Replace with tests for the new API client (src/lib/api.ts) and
// TanStack Query hook contracts once the frontend migration is complete.
